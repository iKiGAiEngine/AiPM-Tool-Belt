Invite-Only Auth — AiPM
Stack: React + TypeScript, Express, PostgreSQL, Drizzle ORM, express-session. No JWT. No new auth systems. No new email libraries.
Email: SendGrid via SENDGRID_API_KEY. Sender: AiPM / no-reply@aipmapp.com. Reset subject: “Reset your AiPM password.” Invite subject: “You’ve been invited to AiPM — set your password.”
Existing systems to extend — do not rebuild. Auth: extend current express-session system. Users: extend existing Admin Dashboard Users table — Add User flow already exists.
Schema additions to existing users table — add exactly these columns: status enum: active | invited | inactive, default invited. password_hash text nullable. reset_token text nullable. reset_token_expires_at timestamp nullable.
Login: email + password only. Generic error: “Invalid email or password.” Never reveal which field failed. Only active users can log in. Invited and inactive are rejected. No public registration, no sign-up link, no self-service account creation anywhere in the UI. Login page note: “Access is by authorized invite only. Contact your administrator for access.”
Invite flow: When admin adds a user via Add User, set status to invited and send SendGrid invite email with secure set-password link. When user sets password via invite link, status automatically updates to active. No default passwords assigned to anyone.
Password reset: Forgot password for existing users only. Always return the same success message — never reveal if email exists. Reset via secure single-use token, expires 1 hour. Authenticated change password available in account/security settings.
Security: bcrypt minimum 12 rounds or Argon2, never plain text. Rate limit login 10 attempts per 15 min per IP via express-rate-limit. Rate limit forgot-password 5 attempts per 15 min per IP. Protect all authenticated routes.
UI changes — these pages only: Login, Forgot Password, Reset/Set Password, Account/Security settings. Match existing styling exactly. Mobile-friendly. Success/error/loading states on all forms. No sign-up links anywhere. Do not add MFA, SSO, OAuth, public registration, or new admin dashboards.
Dev/test seed only — never in production — gate behind NODE_ENV=development: test-admin@aipmapp.com / NBS4130 / active / role admin. test-user@aipmapp.com / NBS4130 / active / role user. test-invited@aipmapp.com / no password / invited.
Before writing any code return a plan and stop. Include: implementation approach, existing auth and registration paths found, files to change, exact schema changes, dependencies to install. Do not write any code until I reply “approved.”
After approval return in this order: Plan, Auth reused/extended, Registration paths removed, Files changed, Schema changes, API changes, UI changes, Security notes, Test results, V2 deferrals.​​​​​​​​​​​​​​​​